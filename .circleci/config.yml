version: 2
jobs:
  build:
    docker:
    - image: circleci/buildpack-deps:stretch
      environment:
        IMAGE_URI: 754256621582.dkr.ecr.eu-west-2.amazonaws.com/imran-test/test:rails
    steps:
    - checkout
    - setup_remote_docker
    - run:
        name: Build application Docker image
        command: |
          docker build -t "${IMAGE_URI}" rails-app